---
:backends: yaml
:yaml:
  :datadir: /vagrant/puppet/hieradata
:hierarchy: 
  - "%{::fqdn}"
  - "%{::environment}"
  - common
  - name: "kernel"
    backend: yaml
    path: "kernel/%{facts.kernel}"
